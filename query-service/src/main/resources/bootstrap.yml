spring:
  application:
    name: data-query-service
  profiles:
    active: data_query_service
  cloud:
    config:
      name: query-service,config-client
      uri: http://localhost:8888
      username: spring_cloud_user
      password: springCloud_Pwd1!