spring:
  application:
    name: reactive-query-client
  profiles:
    active: reactive_query_client
  cloud:
    config:
      name: reactive-query-client,config-client
      uri: http://localhost:8888
      username: spring_cloud_user
      password: springCloud_Pwd1!