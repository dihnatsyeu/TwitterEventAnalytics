spring:
  application:
    name: stream-read-service
  profiles:
    active: stream_reader
  cloud:
    config:
      name: stream-read-service,config-client
      uri: http://localhost:8888
      username: spring_cloud_user
      password: springCloud_Pwd1!